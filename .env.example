# Local dev: leave unset to use SQLite (./stadsspel.db).
# Production (Vercel): set this to a Postgres URL (Vercel Postgres, Supabase, Neon, …).
# Both `postgres://` and `postgresql://` are accepted.
DATABASE_URL=
